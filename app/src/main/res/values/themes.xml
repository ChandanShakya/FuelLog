<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FuelLog" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Minimal platform theme for Jetpack Compose, no external dependencies needed -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.FuelLog" parent="Base.Theme.FuelLog" />
</resources>
