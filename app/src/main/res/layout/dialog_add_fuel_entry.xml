<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dialog_padding">

        <!-- Date row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="14sp" />

            <ImageButton
                android:id="@+id/btn_pick_date"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_calendar"
                android:contentDescription="@string/pick_date" />
        </LinearLayout>

        <EditText
            android:id="@+id/edit_odometer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:hint="Odometer"
            android:inputType="numberDecimal"
            android:singleLine="true" />

        <!-- Input mode toggle using RadioGroup -->
        <RadioGroup
            android:id="@+id/toggle_input_mode"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/btn_mode_vol_rate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:checked="true"
                android:text="@string/mode_vol_rate"
                android:textSize="12sp" />

            <RadioButton
                android:id="@+id/btn_mode_vol_cost"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/mode_vol_cost"
                android:textSize="12sp" />

            <RadioButton
                android:id="@+id/btn_mode_rate_cost"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/mode_rate_cost"
                android:textSize="12sp" />
        </RadioGroup>

        <!-- Volume field -->
        <EditText
            android:id="@+id/edit_volume"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:hint="Fuel Volume"
            android:inputType="numberDecimal"
            android:singleLine="true" />

        <!-- Rate field -->
        <EditText
            android:id="@+id/edit_rate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:hint="Rate"
            android:inputType="numberDecimal"
            android:singleLine="true" />

        <!-- Cost field -->
        <EditText
            android:id="@+id/edit_cost"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:hint="Total Cost"
            android:inputType="numberDecimal"
            android:singleLine="true" />

        <!-- Computed value display -->
        <TextView
            android:id="@+id/text_computed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:textSize="16sp"
            android:textColor="?attr/colorPrimary" />
    </LinearLayout>
</ScrollView>
